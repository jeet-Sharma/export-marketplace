/** Badge/StatusPill color tone — must match the keys of TONE_CLASSES in Badge.tsx. */
export type BadgeTone = "neutral" | "saffron" | "teal" | "blueGrey" | "coral";

/** Button visual variant — must match the keys of VARIANT_CLASSES in Button.tsx. */
export type ButtonVariant = "primary" | "accent" | "ghost" | "danger";

/** Button size — must match the keys of SIZE_CLASSES in Button.tsx. */
export type ButtonSize = "sm" | "md";
